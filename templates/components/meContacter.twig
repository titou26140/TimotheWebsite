<section id="meContacter">
    <div class="container">
        <div class="row">
            <div class="col">
                <div class="title-zone">
                    <h2>Me contacter</h2>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col">
                <div class="ilustration-zone">
                    <img src="{{ asset('/build/images/me-contacter/letter.svg') }}" alt="Letter">
                </div>
            </div>
            <div class="col">
                {{ form_start(contactForm) }}
                {{ form_errors(contactForm) }}
                {{ form_row(contactForm.emailFrom) }}
                {{ form_row(contactForm.object) }}
                {{ form_row(contactForm.content) }}
                <input class="btn btn-success" type="submit" value="Envoyer">
                {{ form_end(contactForm) }}
            </div>
        </div>
    </div>
</section>
